let names = ["Ariana", "Jesse", "Robert", "Fernanda","Tato"];
let grades = [100,100,100,60,30];
// how to access the values unsing the index
names[0]
console.log(names[0]);

// how many elements
console.log(names.length);

//object is how u describe something.